const assert = require('assert')
const blockchain = require('./blockchain')

suite('blockchain', () => {
  test('createMerkle', () => {
    assert(blockchain)
  })

  test('createBlock', () => {
    assert(true)
  })
})

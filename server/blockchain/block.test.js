const assert = require('assert')
const block = require('./block')

suite('block', () => {
  test('createMerkle', () => {
    assert(block)
  })

  test('createBlock', () => {
    assert(true)
  })
})

const assert = require('assert')
const blockchain = require('./block')

suite('blockchain', () => {
  test('createMerkle', () => {
    assert(blockchain)
  })

  test('createBlock', () => {
    assert(true)
  })
})

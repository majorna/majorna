{
  "engines": {
    "node": ">=10 <11"
  },
  "scripts": {
    "start": "[[ $DEVENVS_SH ]] && source $DEVENVS_SH; node index.js",
    "test": "[[ $DEVENVS_SH ]] && source $DEVENVS_SH; npm run lint && NODE_ENV=test mocha **/*test.js --recursive --timeout 60s --ui tdd",
    "lint": "standard --fix",
    "deploy": "cd .. && git subtree push --prefix server heroku master",
    "deploy-force": "cd .. && git push heroku `git subtree split --prefix server master`:master --force"
  },
  "dependencies": {
    "@google-cloud/logging": "4.0.1",
    "@octokit/rest": "15.13.1",
    "axios": "0.18.0",
    "firebase-admin": "6.0.0",
    "kcors": "2.2.2",
    "koa": "2.5.3",
    "koa-bodyparser": "4.2.1",
    "koa-logger": "3.2.0",
    "koa-route": "3.2.0",
    "merkle-tools": "1.4.0"
  },
  "devDependencies": {
    "firebase": "5.5.4",
    "mocha": "5.2.0",
    "nyc": "13.1.0",
    "standard": "12.0.1"
  },
  "standard": {
    "env": [
      "mocha"
    ]
  }
}

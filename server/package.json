{
  "name": "server",
  "main": "index.js",
  "engines": {
    "node": ">=9.4"
  },
  "scripts": {
    "start": "node index.js",
    "test": "npm run lint && NODE_ENV=test nyc mocha **/*test.js --recursive --timeout 30s --ui tdd",
    "lint": "standard --fix"
  },
  "dependencies": {
    "@octokit/rest": "^14.0.8",
    "axios": "^0.17.1",
    "firebase-admin": "^5.8.1",
    "kcors": "^2.2.1",
    "koa": "^2.4.1",
    "koa-bodyparser": "^4.2.0",
    "koa-logger": "^3.1.0",
    "koa-route": "^3.2.0",
    "merkle-tools": "^1.4.0"
  },
  "devDependencies": {
    "mocha": "^5.0.0",
    "nyc": "^11.4.1",
    "standard": "^10.0.3"
  },
  "standard": {
    "env": [
      "mocha"
    ]
  }
}

service cloud.firestore {
  match /databases/{database}/documents {
    // users can read their own document
    match /users/{userId} {
      allow read: if request.auth.uid == userId;
    }

    // exchange data is public
    match /mj/exchange/usd/monthly {
      allow read;
    }
  }
}
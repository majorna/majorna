{
  "devDependencies": {
    "firebase-tools": "3.18.6"
  },
  "scripts": {
    "start": "NODE_ENV=development npm start --prefix server & react-scripts start",
    "build": "npm run build --prefix ui",
    "test": "npm test --prefix server && react-scripts test --env=jsdom --coverage",
    "deploy": "npm run build && firebase deploy && npm run deploy --prefix server"
  }
}
